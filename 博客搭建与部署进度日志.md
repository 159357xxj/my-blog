# 个人 Hexo 博客搭建与部署进度日志

> 记录范围：2026-09-17 至 2026-09-19  
> 本地项目：C:\Users\ASUS\myblog  
> GitHub 仓库：https://github.com/159357xxj/my-blog  
> 线上地址：https://my-blog-33h.pages.dev/  
> 主分支：main

## 1. 当前状态

博客已经可以正常访问，并已通过 GitHub 与 Cloudflare Pages 自动部署。

当前首页使用 Redefine 主题作为博客骨架，并加入了自定义 Aurora 极光背景和粒子文字首屏效果。最新已发布提交为：

~~~
b4e0813 Add particle hero homepage
~~~

在线文章管理后台的代码已经完成（见第 8 节），推送后还需在 GitHub 和 Cloudflare 各做一次一次性配置（创建 OAuth App、设置环境变量），后台即可正式使用。

## 2. 已完成事项总览

| 项目 | 状态 | 说明 |
| --- | --- | --- |
| Node.js 与 Hexo CLI | 已完成 | 已具备本地创建、预览和构建 Hexo 博客的环境。 |
| Hexo 项目初始化 | 已完成 | 项目目录为 C:\Users\ASUS\myblog。 |
| 依赖安装 | 已完成 | Hexo、Redefine 主题、文章统计等依赖已安装。 |
| 本地预览 | 已完成 | 可以通过本地服务器预览博客。 |
| Git 仓库初始化 | 已完成 | 本地分支为 main，已配置用户名和邮箱。 |
| GitHub 远程仓库 | 已完成 | 已连接到 159357xxj/my-blog。 |
| Cloudflare Pages | 已完成 | 已绑定 GitHub 仓库并自动部署。 |
| 生产地址配置 | 已完成 | Hexo 的 url 已设为 https://my-blog-33h.pages.dev。 |
| 博客主题 | 已完成 | 最终主题为 hexo-theme-redefine。 |
| 首页粒子首屏 | 已完成 | Aurora 背景、粒子标题、鼠标互动和副标题形变已上线。 |
| 在线后台发文 | 代码已完成 | Decap CMS + GitHub OAuth + Cloudflare Pages Function；待配置环境变量后生效。 |

## 3. 环境与项目初始化

### 3.1 安装 Hexo CLI

已安装 Node.js 环境及全局 Hexo 命令行工具。核心命令如下：

~~~powershell
npm install -g hexo-cli
~~~

### 3.2 初始化博客项目

在用户目录下初始化 Hexo：

~~~powershell
cd C:\Users\ASUS
hexo init myblog
cd myblog
npm install
~~~

初始化时曾受到 GitHub 网络连接影响，主题模板克隆出现问题；但 Hexo 已创建基础项目文件，之后安装依赖并正常运行。

### 3.3 本地启动与生成静态文件

初始预览使用：

~~~powershell
hexo s
~~~

当前项目 package.json 中已经提供了等价脚本：

~~~powershell
npm run server
npm run clean
npm run build
~~~

其中：

- npm run server：启动本地预览，默认访问地址为 http://localhost:4000。
- npm run clean：删除 Hexo 生成缓存和旧 public 文件。
- npm run build：生成静态网页到 public 文件夹。

## 4. Git 与 GitHub 配置

### 4.1 .gitignore

已在项目根目录创建 .gitignore，避免上传依赖目录、构建产物和缓存文件：

~~~
node_modules/
public/
.deploy_git/
db.json
*.log
.DS_Store
Thumbs.db
~~~

public 被忽略是正常设计。Cloudflare Pages 会在云端执行构建，因此不需要把 public 提交到 GitHub。

### 4.2 初始化 Git 并设置身份

~~~powershell
cd C:\Users\ASUS\myblog
git init -b main
git config --global user.name "chenyiping"
git config --global user.email "3424129289@qq.com"
~~~

### 4.3 首次提交并推送

~~~powershell
git add .
git commit -m "Initial Hexo blog"
git remote add origin https://github.com/159357xxj/my-blog.git
git push -u origin main
~~~

首次成功提交：

~~~
7b4abcb Initial Hexo blog
~~~

## 5. Cloudflare Pages 部署

### 5.1 部署模式

当前使用 GitHub 自动部署模式：

1. 本地修改 Hexo 源码或文章。
2. 提交并推送到 GitHub main 分支。
3. Cloudflare Pages 检测到新提交。
4. Cloudflare 在云端执行构建。
5. 生成的 public 静态网站自动发布到 pages.dev。

因此，日常不需要手动上传 public 文件夹。

### 5.2 生产站点地址

已在 C:\Users\ASUS\myblog\_config.yml 中配置：

~~~
url: https://my-blog-33h.pages.dev
~~~

对应提交：

~~~
484c84f Configure production site URL
~~~

## 6. 主题与首页改造

### 6.1 主题迭代

曾试用 Fluid 主题以改善 Hexo 默认外观。经过对比后，最终改为 Redefine 主题作为完整博客结构，当前项目配置为：

~~~
theme: redefine
~~~

当前依赖中包含：

~~~
hexo-theme-redefine
hexo-wordcount
~~~

对应主题重构提交：

~~~
d76ce0e Redesign blog with Redefine theme
~~~

### 6.2 粒子文字首屏

在保留 Redefine 导航、搜索、文章列表、归档、标签、侧栏等能力的前提下，已完成首页首屏定制：

- 全屏 Aurora 极光 Canvas 动态背景。
- 主标题：陈一平的博客。
- 副标题在“代码 · 学习 · 生活”和“记录问题，也记录答案”之间以粒子形变切换。
- 页面加载时，粒子从随机位置汇聚为文字。
- 鼠标接近粒子时，粒子会受排斥散开；鼠标离开后回归文字轮廓。
- 首屏以下仍保持文章列表和正常滚动布局。
- 尊重 prefers-reduced-motion 设置，在减少动画模式下显示可读的静态文字。

涉及的主要文件：

| 文件 | 作用 |
| --- | --- |
| _config.redefine.yml | Redefine 主题覆盖配置，关闭默认的逐字副标题等冲突效果。 |
| source/css/site-enhancements.css | Aurora、粒子画布、首屏层级和文章卡片视觉增强样式。 |
| source/js/site-enhancements.js | 粒子汇聚、鼠标排斥、文字形变、页面切换时的单例管理逻辑。 |

构建验证已执行：

~~~powershell
npm run clean
npm run build
node --check source/js/site-enhancements.js
git diff --check
~~~

上线提交：

~~~
b4e0813 Add particle hero homepage
~~~

推送成功后，已确认线上站点返回 HTTP 200，且线上 JavaScript 已包含 ParticleHero、主标题和两组副标题内容。

## 7. 以后发布文章的标准流程

### 7.1 新建文章

~~~powershell
cd C:\Users\ASUS\myblog
hexo new post "my-first-post"
~~~

文章会创建在：

~~~
source/_posts/my-first-post.md
~~~

### 7.2 编辑文章

~~~powershell
notepad source\_posts\my-first-post.md
~~~

文章基础格式：

~~~markdown
---
title: 我的第一篇文章
date: 2026-09-19 20:00:00
tags:
  - 学习
categories:
  - 博客
---

# 开始写作

这里是正文。
~~~

### 7.3 本地预览

~~~powershell
npm run clean
npm run build
npm run server
~~~

浏览器打开 http://localhost:4000。确认无误后，在 PowerShell 中按 Ctrl + C 停止预览服务。

### 7.4 发布上线

以文章文件 my-first-post.md 为例：

~~~powershell
git add source\_posts\my-first-post.md
git commit -m "Publish my first post"
git push origin main
~~~

推送成功后等待约 30 到 90 秒，Cloudflare Pages 会自动完成部署。

## 8. 在线管理后台（代码已完成）

### 8.1 已实现的组成

| 文件 | 作用 |
| --- | --- |
| source/admin/index.html | 加载 Decap CMS 界面，作为 /admin/ 入口。 |
| source/admin/config.yml | Decap 配置：GitHub 后端、main 分支、文章集合（标题/日期/标签/分类/正文）。 |
| functions/api/auth.js | Pages Function，OAuth 第一步：跳转 GitHub 授权页。 |
| functions/api/auth/callback.js | Pages Function，OAuth 第二步：code 换 token、校验站主身份、安全回传 token。 |
| _config.yml | skip_render 排除 admin 目录，admin 文件原样发布。 |
| _config.redefine.yml | 导航栏最右新增“管理”入口（/admin/）。 |

安全设计：

- GitHub Client Secret 只存在于 Cloudflare 环境变量，code 换 token 在服务端完成，前端拿不到 secret。
- callback 会调用 GitHub API 校验登录者用户名，只有 ALLOWED_GITHUB_LOGIN 指定的账号（默认 159357xxj）能拿到 token，其他 GitHub 账号会被拒绝。
- 游客打开 /admin/ 只能看到 Decap 登录页，没有任何内容权限。
- 发布文章即提交 Markdown 到 GitHub main，Cloudflare Pages 自动构建部署，Git 历史完整保留。

### 8.2 启用步骤（一次性配置）

1. 打开 https://github.com/settings/developers ，新建 OAuth App：
   - Application name：随意，如 my-blog-admin
   - Homepage URL：https://my-blog-33h.pages.dev
   - Authorization callback URL：https://my-blog-33h.pages.dev/api/auth/callback
   - 创建后记下 Client ID，并生成一个 Client Secret。
2. 打开 Cloudflare Dashboard → Workers & Pages → my-blog-33h → Settings → Variables and Secrets，添加三个变量（Production 与 Preview 都要）：
   - GITHUB_CLIENT_ID = OAuth App 的 Client ID
   - GITHUB_CLIENT_SECRET = OAuth App 的 Client Secret
   - ALLOWED_GITHUB_LOGIN = 159357xxj
3. 保存后重新部署一次（推送任意提交，或在 Deployments 里 Retry），环境变量即可生效。
4. 访问 https://my-blog-33h.pages.dev/admin/ ，点击“Login with GitHub”，用 159357xxj 账号授权后即可新建、编辑、发布文章。

## 9. 当前可用链接与关键路径

| 项目 | 地址或路径 |
| --- | --- |
| 线上博客 | https://my-blog-33h.pages.dev/ |
| GitHub 仓库 | https://github.com/159357xxj/my-blog |
| 本地博客项目 | C:\Users\ASUS\myblog |
| 文章目录 | C:\Users\ASUS\myblog\source\_posts |
| 主题配置 | C:\Users\ASUS\myblog\_config.redefine.yml |
| 自定义样式 | C:\Users\ASUS\myblog\source\css\site-enhancements.css |
| 自定义脚本 | C:\Users\ASUS\myblog\source\js\site-enhancements.js |

## 10. 注意事项

- Git 提示 “LF will be replaced by CRLF” 是 Windows 换行符提示，不是提交失败。
- public、node_modules 和 db.json 被忽略是预期行为，不需要手动上传。
- 若访问网站时仍看到旧样式，可使用 Ctrl + F5 强制刷新浏览器缓存。
- Git 推送出现临时网络错误时，可以直接再次执行 git push origin main。
- 当前博客已经上线；在线后台发文功能尚处于方案确认阶段，尚未部署。
